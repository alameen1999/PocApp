import {StyleSheet} from 'react-native';

export const profileStyle = StyleSheet.create({
  headerText: {
    color: 'white',
    fontSize: 20,
    fontWeight: 'bold',
  },
  profileContainer: {
    flexDirection: 'row',
  },
});
